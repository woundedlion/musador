PROJECT(sqlite)
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

ADD_LIBRARY(${PROJECT_NAME} sqlite3.h sqlite3.c)

ADD_DEFINITIONS(-DSQLITE_THREADSAFE=2)

SET_TARGET_PROPERTIES(${PROJECT_NAME} PROPERTIES COMPILE_FLAGS /w)